spring:
  application:
    name: mcn
  data:
    mongodb:
      port: 0
      database: test
  mongock:
    change-logs-scan-package:
      - me.local.mcn.changelogs
  mail:
    host: test
    port: 2222
    username: test
    password: test
    protocol: smtps
    properties:
      mail:
        smtp:
          auth: true
          starttls.enable: true
server:
  port: 8080